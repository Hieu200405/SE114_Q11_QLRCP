<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="@drawable/background_film_show"
    android:layout_margin="8dp"
    android:elevation="2dp">

    <!-- Giờ chiếu -->
    <TextView
        android:id="@+id/textTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="06:00"
        android:textColor="@color/colorTextPrimary"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Giá vé -->
    <TextView
        android:id="@+id/textPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="100.000 đ"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/colorAccent"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/textTime" />

    <!-- Ngày chiếu -->
    <TextView
        android:id="@+id/textDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="10/10/2025"
        android:textSize="14sp"
        android:textColor="@color/colorTextSecondary"
        android:layout_below="@id/textTime"
        android:layout_marginTop="4dp" />

    <!-- Phòng chiếu và số ghế -->
    <TextView
        android:id="@+id/textRoomSeats"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Phòng 2 • 20 ghế"
        android:textSize="14sp"
        android:textColor="@color/colorTextSecondary"
        android:layout_below="@id/textDate"
        android:layout_marginTop="2dp" />

    <!-- Thông tin rạp chiếu và khoảng cách -->
    <TextView
        android:id="@+id/textCinemaInfo"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/textRoomSeats"
        android:layout_marginTop="2dp"
        android:layout_toStartOf="@+id/btnNavigate"
        android:layout_alignParentStart="true"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="🎬 CGV Vincom • 5.2 km (~15 phút)"
        android:textColor="#4CAF50"
        android:textSize="13sp" />

    <!-- Nút điều hướng -->
    <ImageView
        android:id="@+id/btnNavigate"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:padding="6dp"
        android:src="@drawable/ic_navigate"
        android:background="@drawable/bg_circle_button"
        android:contentDescription="Navigate"
        android:layout_alignParentEnd="true"
        android:layout_below="@id/textRoomSeats"
        android:layout_marginTop="2dp"
        app:tint="#4CAF50" />

    <!-- Nút Xóa -->
    <ImageButton
        android:id="@+id/buttonDelete"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_below="@id/btnNavigate"
        android:layout_alignEnd="@id/btnNavigate"
        android:layout_marginTop="4dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_delete"
        android:visibility="gone"
        android:contentDescription="Xóa"
        android:scaleType="centerInside"
        app:tint="#DE4E4E" />
</RelativeLayout>
